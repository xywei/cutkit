## 1. OpenSpec Artifacts

- [x] 1.1 Add proposal/design/spec/tasks artifacts for multipatch interface
  coupling rollout.

## 2. IR + Parser Descriptor Support

- [x] 2.1 Add deterministic multipatch patch/interface descriptor parsing and IR
  metadata threading.
- [x] 2.2 Add payload-validation diagnostics for missing or malformed
  multipatch descriptor keys.

## 3. IGA Interface Lowering

- [x] 3.1 Add capability-gated multipatch interface lowering on backend `iga`.
- [x] 3.2 Normalize interface orientation deterministically and expose stable
  execution-path metadata for multipatch lowering.

## 4. Validation + Docs

- [x] 4.1 Add regression tests for valid multipatch interface coupling and
  deterministic diagnostics.
- [x] 4.2 Update docs and execution-plan progress with multipatch rollout
  semantics.
