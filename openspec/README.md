# OpenSpec Artifacts

This directory contains spec-driven change artifacts managed by OpenSpec.

- `openspec/changes/`: active and archived changes
- `openspec/specs/`: accepted and current specification documents

Use `/opsx:propose`, `/opsx:apply`, and `/opsx:archive` from supported agents.
