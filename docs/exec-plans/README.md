# Execution Plans

Execution plans track non-trivial implementation work.

- Place in-progress plans in `active/`.
- Move completed plans to `completed/` once merged.

Suggested filename:

`YYYYMMDD-<short-kebab-title>.md`
